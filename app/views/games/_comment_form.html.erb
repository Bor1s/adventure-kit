<%= form_for [@game, @game.comments.build], html: { role: 'form' } do |f| %>
  <div class="form-group">
    <%= f.text_area :message, class: 'form-control', placeholder: 'Message', size: '5x5' %>
  </div>
  <div class="form-group">
    <%= f.submit class: 'btn btn-success' %>
  </div>
<% end %>
